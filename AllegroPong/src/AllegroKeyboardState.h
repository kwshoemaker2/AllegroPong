#pragma once
#include <allegro5/allegro.h>
#include "AllegroPong.h"
#include "IEventSource.h"

namespace Pong {
    ////////////////////////////////////////////////////////////////////////////
    class AllegroKeyboardState
    {
    public:
        ////////////////////////////////////////////////////////////////////////
        AllegroKeyboardState() = default;

        ////////////////////////////////////////////////////////////////////////
        ~AllegroKeyboardState() = default;

        ////////////////////////////////////////////////////////////////////////
        bool Refresh();

        ////////////////////////////////////////////////////////////////////////
        bool KeyIsDown(INT32 allegroKeyCode) const;

    private:
        ALLEGRO_KEYBOARD_STATE mKeyboardState = { 0 };
    };

}